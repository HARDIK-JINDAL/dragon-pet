<h1>🦖 Dragon Desktop Pet</h1><p>A small, animated <strong>8-bit desktop pet</strong> that lives on your Windows taskbar. It bobs, walks left ↔ right, and bounces when clicked — and disappears cleanly when you close the app.</p><p>Built entirely in <strong>Python + PyQt5 + Pillow</strong> — no backend, no system mods, just pure fun.</p><hr><h2>🎮 Features</h2><ul><li>🟠animated GIF </li><li>🎢 Smooth side-to-side motion with soft bobbing</li><li>🪄 Click to make it bounce high</li><li>🧊 Transparent background (no window frame)</li><li>🚫 No permanent changes to your system</li><li>❌ Closes instantly when the program stops</li><li>⚙️ All-in-one single Python file</li></ul><hr><h2>🧰 Requirements</h2><p>Make sure you have <strong>Python 3.9+</strong> installed.</p><pre><code>pip install pyqt5 pillow</code></pre><hr><h2>🚀 How to Run</h2><ol><li>Put your pet GIF (e.g. <code>dino.gif</code>) in the same folder as the script. <em>(Transparent background recommended for best visuals.)</em></li><li>Run the pet:<pre><code>python pet.py</code></pre></li><li>Your dino will appear on your taskbar — walking, bobbing, and bouncing! Close the window / stop the script → the pet vanishes. 🦕</li></ol><hr><h2>🧱 Folder Structure</h2><pre><code>📂 DinoPet/<br> ├─ pet.py   # main script<br> ├─ dino.gif                    # your GIF pet<br> ├─ RunDinoPet.bat              # optional launcher<br> ├─ README.md                   # this file</code></pre><hr><h2>⚙️ Optional Setup</h2><h3>🟢 1. Quick Launcher</h3><p>Create a file named <code>RunDinoPet.bat</code> in the same folder:</p><pre><code>@echo off<br>titleDino Pet<br>python "%~dp0pet.py"<br>pause</code></pre><p>Double-click <code>RunDinoPet.bat</code> to start your pet instantly. Closing the terminal stops the pet.</p><h3>🟣 2. Run Silently (no console)</h3><p>Make a Windows shortcut using <code>pythonw.exe</code>:</p><pre><code>pythonw.exe "C:\path\to\pet.py"</code></pre><p>No terminal window — pet appears immediately. Closing the process removes it.</p><h3>🔵 3. Build a Standalone EXE</h3><p>To run without Python installed:</p><pre><code>pip install pyinstaller<br>pyinstaller --noconsole --onefile pet.py</code></pre><p>You’ll get:</p><pre><code>dist/pet.exe</code></pre><p>Place your <code>dino.gif</code> beside it. Double-click → pet appears. Exit the process → pet disappears.</p><hr><h2>🧮 Configuration</h2><p>Edit constants at the top of <code>pet.py</code> to tweak behavior:</p><table><tr><th>Variable</th><th>Description</th><th>Default</th></tr><tr><td><code>SCALE</code></td><td>Pet size multiplier</td><td><code>0.45</code></td></tr><tr><td><code>WALK_SPEED</code></td><td>Average walking speed</td><td><code>2.4</code></td></tr><tr><td><code>BOUNCE_HEIGHT</code></td><td>Height of bounce on click</td><td><code>140</code></td></tr><tr><td><code>PAUSE_MIN</code> / <code>PAUSE_MAX</code></td><td>Short pause duration at each stop</td><td><code>0.18s – 0.95s</code></td></tr><tr><td><code>RAISE_PET</code></td><td>Vertical offset from taskbar</td><td><code>-40</code></td></tr></table><hr><h2>🧠 Notes</h2><ul><li>The script never edits your system, registry, or startup settings.</li><li>The pet exists only while the Python process is running.</li><li>It floats <em>above</em> other windows but doesn’t block clicks underneath.</li><li>Tested on <strong>Windows 10/11</strong> (should work anywhere PyQt5 runs).</li></ul><hr><h2>❤️ Credits</h2><p>Developed with love and curiosity by <strong>Hardik</strong>, for everyone who wanted a tiny 8-bit companion on their screen. 💻🧡</p><hr><h2>🧡 License</h2><p>MIT License — free to modify, learn from, and share.</p>
